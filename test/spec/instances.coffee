# describe 'Instances', ->

#   it 'should be defined', ->
#     # expect(bouncy.Instances.getBall()).toBe 'hello'

# # describe 'Instances', ->

# #   beforeEach ->
# #     @instances = new bouncy.Instances()

# #   it 'should be defined', ->
# #     expect(@instances).toBeDefined()

# #   it 'should be able to get balls', ->
# #     expect(@instances.getBalls().length).toBe 0

# #   it 'should be able to create balls', ->
# #     @instances.createBall 0, 0
# #     expect(@instances.getBalls().length).toBe 1

# #   it 'should be a singleton', ->
# #     @instances.destroyBalls()
# #     expect(@instances.getBalls().length).toBe 0
# #     @instances2 = new bouncy.Instances()
# #     expect(@instances2.getBalls().length).toBe 0
# #     @instances2.createBall 0, 0
# #     expect(@instances2.getBalls().length).toBe 1
